import '@babel/polyfill';

import * as utils from '@picimo/utils';
import * as core from '@picimo/core';
import * as renderer from '@picimo/renderer';
import * as toolkit from '@picimo/toolkit';
import * as ecs from '@picimo/ecs';

import eventize from '@spearwolf/eventize';
// import * as glMatrix from 'gl-matrix';

export {
  core,
  ecs,
  eventize,
  // glMatrix,
  renderer,
  toolkit,
  utils,
};
